import "./Form.css";

function Form(props) {
  return(
    <form>
      <input type="text" placeholder="Search For A Movie..."></input>
      <button>SEARCH</button>
    </form>
  );
};

export default Form;